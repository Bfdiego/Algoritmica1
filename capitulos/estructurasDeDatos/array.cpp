#include<bits/stdc++.h>
using namespace std;

int main() {
    int array[10];
     
    memset(array, 0, 10);
    int i;
    for(i = 0; i< 10; i++) {
        array[i] = 0;
    }
    //lectura O(n) = 1 
    array[i];

    // escritura O(n) = 1
    array[0] = 10;

    return 0;
}